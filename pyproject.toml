[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ura-ocr"
version = "0.1.0"
description = "OCR and brand/product extraction pipeline for URA Hackathon"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]